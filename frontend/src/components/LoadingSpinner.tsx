import { ImSpinner9 } from 'react-icons/im';
import type { IconBaseProps } from 'react-icons/lib';

export default function LoadingSpinner(props: IconBaseProps) {
    return <ImSpinner9 {...props} />;
}
